# aiify-frontend (Vue 3 + Vite + Tailwind + Vuex)

Quick start (dev)
1. npm install
2. copy `.env.development` and fill your SUPABASE keys
3. npm run dev (runs at http://localhost:5173)


Notes: Assets from your figma export were copied into `public/assets/` and used as visual references in the pages.
